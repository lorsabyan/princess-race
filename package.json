{
  "name": "princess-race",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "DEPLOY_BASE=/princess-race/ npm run build && git -C dist init -b gh-pages && git -C dist add -A && git -C dist commit -m 'Deploy to GitHub Pages' && git -C dist push -f https://github.com/lorsabyan/princess-race.git gh-pages && rm -rf dist/.git"
  },
  "dependencies": {
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.0"
  }
}
